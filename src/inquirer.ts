import inquirer, { Answers } from "inquirer";
import { questions } from "./utils/questions";

const questionsFunc = async () => {
  //   const answers: Answers = console.log(answers);
  //   return answers;
};

questionsFunc();
